{
   "description": "MUTABLE metadata for input data source -- not the actual data content",
   "properties": {
      "id": {
         "type": "integer"
      },
      "owner_id": {
         "type": "integer"
      },
      "sha256": {
         "type": "string"
      },
      "size": {
         "type": "integer"
      },
      "sourcePath": {
         "type": "string"
      },
      "updatedAt": {
         "type": "string"
      }
   },
   "required": [
      "id",
      "sourcePath",
      "updatedAt",
      "sha256",
      "size"
   ],
   "type": "object"
}
