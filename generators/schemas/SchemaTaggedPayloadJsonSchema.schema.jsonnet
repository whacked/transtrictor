{
  title: 'SchemaTaggedPayloadJsonSchemaSchema',
  type: 'object',
  description: 'json schema for the schema SchemaTaggedPayload trasnformation objects. The schema for the storage of their scehams. Yes, schema of schema',
  properties: {
    title: {
      type: 'string',
    },
    version: {
      type: 'string',
    },
  },
  required: ['title', 'version'],
}
